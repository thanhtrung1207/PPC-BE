package com.example.PPC.Repository;

import com.example.PPC.Model.FullContract;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FullContractRepo extends JpaRepository<FullContract,Long> {
}
